import React from "react"
import Heading from "../../common/Heading"
import { location } from "../../data/Data"
import "./style.css"

const Location = () => {
  return (
    <>
      <section className='location padding'>
        <div className='container'>
          <Heading title='Serving Communities Across Country' subtitle='At Our Organizations  we are dedicated to making a positive impact in the lives of those around us. Our volunteer-based organization is proud to provide services in multiple cities across Country. Whether you are in France,America,Rusia we are committed to serving our community with compassion, care, and dedication.' />

          <div className='content grid3 mtop'>
            {location.map((item, index) => (
              <div className='box' key={index}>
                <img src={item.cover} alt='' />
                <div className='overlay'>
                  <h5>{item.name}</h5>
                  <p>
                   
                    <label>{item.Services}</label>
                  </p>
                </div>
              </div>
            ))}
          </div>
        </div>
      </section>
    </>
  )
}

export default Location
